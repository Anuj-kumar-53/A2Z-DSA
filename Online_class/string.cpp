#include<bits/stdc++.h>
using namespace std;
int main(){
    string n;
    cin.ignore(); // ignores only the first latter
    cin.ignore(5); // ignores five characters
    getline(cin,n);
    cout<<n;
}